[package]
name = "no_denormals"
authors = ["sintel"]
version = "0.3.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/Sin-tel/no_denormals"
homepage = "https://github.com/Sin-tel/no_denormals"
readme = "README.md"
keywords = ["dsp", "float"]
description = "Temporarily turn off floating point denormals."
categories = ["multimedia::audio"]

[features]
default = []

## Enables f16 support testing on nightly Rust.
## This feature only affects tests and does not change the public API.
_f16_test = []
