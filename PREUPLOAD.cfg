[Hook Scripts]
check = ${REPO_ROOT}/development/tools/external_crates/android_cargo.py run --bin crate_tool --manifest-path ${REPO_ROOT}/development/tools/external_crates/Cargo.toml --release -- preupload-check ${PREUPLOAD_FILES}
